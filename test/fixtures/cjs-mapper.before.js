// top-comment
var x = {
    x: require('lookup-x'),
    y: require('lookup-y'),
}

var x = {
    // comment-x
    x: require('lookup-with-comment-x'),
    // comment-y
    y: require('lookup-with-comment-y'),
}
