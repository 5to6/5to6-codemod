import x from 'lookup-x';
import y from 'lookup-y';
// top-comment
var x = {
    x,
    y,
}

// comment-x
import x from 'lookup-with-comment-x';

// comment-y
import y from 'lookup-with-comment-y';

var x = {
    x,
    y,
}
