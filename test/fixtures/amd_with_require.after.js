import a from 'a';
import b from 'b';
import c from 'c';
import g from 'g';
var d, e, f;

// let's define some things
var myModule = {
    doStuffWithThings(val) {
        return a.morgify(val);
    },
};

export default myModule;
